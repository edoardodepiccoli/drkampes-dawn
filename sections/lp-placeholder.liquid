{% comment %}
  Empty placeholder section for fully custom landing pages.
  Renders nothing. Used as the initial "main" in page.landing.json and page.landing_b2b.json
  so the template is valid while real LP sections are being ported from Horizon.
  Replace with actual LP sections (lp-hero, lp-navbar, etc.) as they get ported.
{% endcomment %}

{% schema %}
{
  "name": "Landing page placeholder",
  "tag": "section",
  "class": "section section--lp-placeholder",
  "settings": []
}
{% endschema %}
